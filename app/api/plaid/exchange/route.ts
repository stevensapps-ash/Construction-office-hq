import {NextResponse} from "next/server";import {plaidClient} from "@/lib/plaid";
export async function POST(req:Request){try{const {public_token}=await req.json();if(!public_token)return NextResponse.json({error:"Missing public token"},{status:400});const r=await plaidClient().itemPublicTokenExchange({public_token});/* Production: encrypt r.data.access_token in tenant-scoped database. Never return it to the browser. */return NextResponse.json({connected:true,item_id:r.data.item_id})}catch(e:any){return NextResponse.json({error:e?.message||"Unable to connect bank"},{status:500})}}
