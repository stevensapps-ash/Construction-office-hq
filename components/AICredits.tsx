"use client";import {AI_FLOW_PRICING,CREDIT_PACKS,MONTHLY_PLAN} from "@/lib/ai-pricing";import CheckoutButton from "@/components/CheckoutButton";
export default function AICredits(){return <div className="module"><div className="moduleTop"><div><h2>AI Credits</h2><p>Simple customer credits backed by measured provider token usage.</p></div></div><article><div className="head"><h3>Monthly plan</h3></div><div className="record"><div><h3>{MONTHLY_PLAN.name}</h3><p>{MONTHLY_PLAN.includedCredits} AI credits included each month</p></div><div><strong>${(MONTHLY_PLAN.priceCents/100).toFixed(2)}/mo</strong><CheckoutButton kind="subscription" label="Start Subscription"/></div></div></article><article><div className="head"><h3>AI usage by feature</h3></div><div className="records">{Object.entries(AI_FLOW_PRICING).map(([k,v])=><div className="record" key={k}><div><h3>{v.label}</h3><p>Planning allowance: up to {v.tokenBudget.toLocaleString()} raw AI tokens</p></div><strong>{v.credits} credit{v.credits===1?"":"s"}</strong></div>)}</div></article><article><div className="head"><h3>Buy more credits</h3></div><div className="creditPacks">{CREDIT_PACKS.map(p=><div className="creditPack" key={p.credits}><b>{p.credits} credits</b><strong>${(p.priceCents/100).toFixed(2)}</strong><span>Secure Moov checkout.</span><CheckoutButton kind="credits" credits={p.credits} label="Buy credits"/></div>)}</div></article><article className="profitRule"><h3>Cost protection</h3><p>Production will record actual provider input/output token usage for every generation, reserve the required customer credits first, and block AI when the balance is too low. Token budgets are safety ceilings for pricing, not claims that every request consumes the full amount.</p></article></div>