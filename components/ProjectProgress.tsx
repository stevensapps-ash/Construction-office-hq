"use client";import {useEffect,useState} from "react";import {CheckCircle2,Circle,Plus,Trash2} from "lucide-react";import {db} from "@/lib/store";import type {Project} from "@/lib/types";type Task={id:string;projectId:string;text:string;done:boolean};const K="cohq-progress";const read=():Task[]=>{try{return JSON.parse(localStorage.getItem(K)||"[]")}catch{return[]}};export default function ProjectProgress(){const [projects,setProjects]=useState<Project[]>([]),[tasks,setTasks]=useState<Task[]>([]),[projectId,setProjectId]=useState(""),[text,setText]=useState("");useEffect(()=>{const p=db.projects();setProjects(p);if(p[0])setProjectId(p[0].id);setTasks(read())},[]);function save(next:Task[]){setTasks(next);localStorage.setItem(K,JSON.stringify(next))}function add(){if(!projectId||!text.trim())return;save([...tasks,{id:crypto.randomUUID(),projectId,text:text.trim(),done:false}]);setText("")}const current=tasks.filter(x=>x.projectId===projectId),done=current.filter(x=>x.done).length,pct=current.length?Math.round(done/current.length*100):0;return <div className="module"><div className="moduleTop"><div><h2>Project Progress</h2><p>Track every stage of the job from preparation through final walkthrough.</p></div><strong>{pct}% Complete</strong></div><label>Project<select value={projectId} onChange={e=>setProjectId(e.target.value)}><option value="">Select project</option>{projects.map(p=><option key={p.id} value={p.id}>{p.name}</option>)}</select></label><div className="progressTrack"><span style={{width:pct+"%"}}/></div><div className="taskAdd"><input value={text} onChange={e=>setText(e.target.value)} onKeyDown={e=>e.key==="Enter"&&add()} placeholder="Add work step, e.g. Rough plumbing complete"/><button className="primary" onClick={add}><Plus size={17}/>Add</button></div><div className="taskList">{current.map(t=><article key={t.id}><button className="taskCheck" onClick={()=>save(tasks.map(x=>x.id===t.id?{...x,done:!x.done}:x))}>{t.done?<CheckCircle2/>:<Circle/>}</button><span className={t.done?"done":""}>{t.text}</span><button className="taskDelete" onClick={()=>save(tasks.filter(x=>x.id!==t.id))}><Trash2 size={16}/></button></article>)}</div></div>}