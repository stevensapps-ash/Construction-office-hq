"use client";import {useEffect,useState} from "react";const K="cohq-notes";export default function NotesManager(){const [text,setText]=useState("");useEffect(()=>setText(localStorage.getItem(K)||""),[]);return <div className="module"><div className="moduleTop"><div><h2>Owner Notes</h2><p>Quick private workspace notes saved on this device.</p></div><button className="primary" onClick={()=>{localStorage.setItem(K,text);alert("Notes saved.")}}>Save Notes</button></div><textarea className="notes" value={text} onChange={e=>setText(e.target.value)} placeholder="Job reminders, customer follow-ups, supply lists..."/></div>}