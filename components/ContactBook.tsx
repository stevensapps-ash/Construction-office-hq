"use client";import {useEffect,useState} from "react";import {Mail,MapPin,Phone,Search} from "lucide-react";import {db} from "@/lib/store";import type {Customer} from "@/lib/types";export default function ContactBook(){const [rows,setRows]=useState<Customer[]>([]),[q,setQ]=useState("");useEffect(()=>setRows(db.customers()),[]);const shown=rows.filter(x=>(x.name+" "+x.phone+" "+x.email+" "+x.address).toLowerCase().includes(q.toLowerCase()));return <div className="module"><div className="moduleTop"><div><h2>Contact Book</h2><p>Customer contact information stays connected to every job they hire you for.</p></div></div><div className="secretarySearch"><Search/><input value={q} onChange={e=>setQ(e.target.value)} placeholder="Search contacts..."/></div><div className="contactGrid">{shown.map(c=><article key={c.id}><div className="contactAvatar">{c.name.slice(0,1).toUpperCase()}</div><h3>{c.name}</h3><p><Phone/>{c.phone||"No phone"}</p><p><Mail/>{c.email||"No email"}</p><p><MapPin/>{c.address||"No address"}</p></article>)}</div></div>}