"use client";import {useState} from "react";
export default function CheckoutButton({kind,credits,label}:{kind:"subscription"|"credits";credits?:number;label:string}){const [busy,setBusy]=useState(false);async function go(){setBusy(true);try{const r=await fetch("/api/payments/checkout",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({kind,credits})});const j=await r.json();if(!r.ok||!j.url)throw new Error(j.error||"Checkout unavailable");location.href=j.url}catch(e:any){alert(e.message)}finally{setBusy(false)}}return <button onClick={go} disabled={busy}>{busy?"Opening checkout…":label}</button>}
